# Python
venv/
__pycache__/
*.pyc

# Databases & runtime data
*.db
*.json
data_cache/

# OS junk
.DS_Store

# Local config — contains your ZIP code and location.
# This file is intentionally excluded from version control.
# To get started, copy config.json.example to config.json and fill it in,
# or just run setup.sh and it will be created for you automatically.
config.json
